# Data / DB
data/
*.db
*.db-shm
*.db-wal
*.parquet

# Logs / monitoring
.codex/
logs/
models/

# Portable project skill adapters; all other .codex state/worktrees stay local.
!.codex/
.codex/*
!.codex/skills/
.codex/skills/*
!.codex/skills/crawler-stall-response/
!.codex/skills/crawler-stall-response/SKILL.md
!.codex/skills/opgg-seed-refresh/
!.codex/skills/opgg-seed-refresh/SKILL.md
!.codex/skills/update-mayhem-site/
!.codex/skills/update-mayhem-site/SKILL.md

# Python
__pycache__/
*.pyc
*.pyo
.venv/
venv/
*.egg-info/
.pytest_cache/

# Build artifacts
dist/
build/
tmp-*

# Local image cache; production payload currently references remote splash URLs.
docs/assets/draft-splash-hd/

# Scratch & local-only artifacts (keep the public repo code-only)
# build logs, image cache, intermediate JSON, and an embedded Chrome profile w/ creds
outputs/
# Portable Markdown reports/architecture are tracked; slide workspaces and exports stay local.
documents/*
!documents/README.md
!documents/personal-data-audit.md
!documents/architecture/
!documents/architecture/*.md
!documents/reports/
documents/reports/*
!documents/reports/*.md
# native Overwolf app (built / distributed separately)
overwolf/
# hand-authored score-override CSV inputs
manual/
# one-off scratch analysis scripts
scripts/_*.py
# graphify knowledge-graph output (regenerable from source)
graphify-out/
# data/report exports (regenerable, ~3MB)
exports/

# Publishable review pages
!docs/semantic-score-review.html

# OS
.DS_Store
Thumbs.db

# Per-user agent settings
.claude/settings.local.json
# Claude Code worktrees are local checkouts; never stage them
.claude/worktrees/
